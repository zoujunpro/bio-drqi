<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bio.drqi.document.mapper.DocCodeSequenceMapper">

    <resultMap id="BaseResultMap" type="com.bio.drqi.document.domain.DocCodeSequence">
        <id property="id" column="id" jdbcType="BIGINT"/>
        <result property="categoryCode" column="category_code" jdbcType="VARCHAR"/>
        <result property="yearMonth" column="year_month" jdbcType="VARCHAR"/>
        <result property="currentSeq" column="current_seq" jdbcType="INTEGER"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <insert id="insertIgnore">
        INSERT IGNORE INTO doc_code_sequence(category_code, `year_month`, current_seq, create_time, update_time)
        VALUES (#{categoryCode}, #{yearMonth}, 0, NOW(), NOW())
    </insert>

    <select id="selectForUpdate" resultMap="BaseResultMap">
        SELECT id, category_code, `year_month`, current_seq, create_time, update_time
        FROM doc_code_sequence
        WHERE category_code = #{categoryCode}
          AND `year_month` = #{yearMonth}
        FOR UPDATE
    </select>

    <update id="updateCurrentSeq">
        UPDATE doc_code_sequence
        SET current_seq = #{currentSeq}, update_time = NOW()
        WHERE id = #{id}
    </update>
</mapper>
